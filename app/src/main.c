/**
 * @file main.c
 * @brief Application entry point - currently an empty stub; task code
 *        will be added here once api/ and rtos/api/ exist.
 */

/**
 * @brief Application entry point.
 * @return Never returns (infinite loop); the int return type matches
 *         the standard C main() signature.
 */
int main(void)
{
  while (1)
  {
  }
  return 0;
}
